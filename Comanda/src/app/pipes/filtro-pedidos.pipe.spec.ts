import { FiltroPedidosPipe } from './filtro-pedidos.pipe';

describe('FiltroPedidosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPedidosPipe();
    expect(pipe).toBeTruthy();
  });
});
